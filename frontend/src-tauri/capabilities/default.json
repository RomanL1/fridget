{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "os:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://fridget-001.prod.projects.ls.eee.intern/**" }, { "url": "http://localhost:8080/**" }]
    }
  ]
}
